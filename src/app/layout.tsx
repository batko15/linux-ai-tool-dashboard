import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Linux AI Tool Dashboard - KDE Plasma Setup",
  description: "Umfassendes Dashboard fuer Linux AI Tools, KDE Plasma 6 Themes, Performance-Optimierungen, Gaming und Desktop-Customization.",
  keywords: ["Linux", "AI Tools", "KDE Plasma", "Ollama", "Stable Diffusion", "Manjaro", "NVIDIA RTX 4070"],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="de" className="dark" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased bg-background text-foreground`}
      >
        {children}
      </body>
    </html>
  );
}
